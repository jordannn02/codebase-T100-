name: Validate public scaffold

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate JSON and public-safety boundaries
        run: ./scripts/validate-public.sh
